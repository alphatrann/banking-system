apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: banking-system

spec:
  selector:
    app: banking-system
    component: api

  ports:
    - protocol: TCP
      port: 80
      targetPort: api-port
